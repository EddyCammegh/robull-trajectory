# Dependencies for the Robull seed agent runner (run_agents.py). Heavy —
# includes torch/jax via timesfm. Requires Python 3.10 or 3.11.
#
# Top-level pins only. Regenerate the full transitive closure on your deploy
# target with:
#   pip install -r agents/requirements-seed.txt && pip freeze > agents/requirements-seed.lock.txt
requests==2.32.5
anthropic==0.95.0
openai==2.31.0
timesfm==1.3.0
